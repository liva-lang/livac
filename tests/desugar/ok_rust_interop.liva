// Rust interop — use rust with version, features, alias
use rust "chrono" version "0.4"
use rust "uuid" version "1.0" features ["v4", "serde"]
use rust "my_lib" as mylib

main() {
    print("Rust interop test")
}
