// Test for return type inference in expression-bodied functions
add(a, b) = a + b
subtract(x, y) = x - y
multiply(n, m) = n * m
divide(p, q) = p / q

main() {
  let sum = add(10, 5)
  let diff = subtract(10, 3)
  let prod = multiply(4, 6)
  let quot = divide(20, 4)
  
  print(sum)
  print(diff)
  print(prod)
  print(quot)
}
