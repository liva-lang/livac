// Test file for goto definition

fn add(x: int, y: int) -> int {
    return x + y
}

fn multiply(a: int, b: int) -> int {
    return a * b
}

class Calculator {
    fn calculate(x: int, y: int) -> int {
        return add(x, y)  // Should jump to add definition
    }
}

type Point = (int, int)

fn main() {
    let result = add(5, 10)       // F12 on 'add' should jump to line 3
    let product = multiply(3, 4)   // F12 on 'multiply' should jump to line 7
    let calc = Calculator{}
    let sum = calc.calculate(1, 2)
}
