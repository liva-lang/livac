Crea una API REST en Liva (.liva) para gestionar una biblioteca de libros, usando rust { } interop para el servidor HTTP. Endpoints: CRUD completo de libros (título, autor, año, ISBN) y autores (nombre, nacionalidad). Relación 1:N entre autor y libros. Búsqueda por título y por autor. Paginación con ?page=1&limit=10. Persiste datos en un archivo JSON usando File.read(), File.write() y JSON.parse()/JSON.stringify(). Incluye validación de inputs y códigos de error HTTP apropiados. Guarda este prompt exacto en un archivo original_prompt.md dentro del proyecto.
