Implementa un intérprete para un mini-lenguaje en Liva (.liva) que soporte: variables (let x = 10), aritmética (+, -, , /), condicionales (if/else), bucles (while), funciones definidas por el usuario, y print. Implementa las fases: lexer (tokenización), parser (genera un AST usando enums con datos), y evaluador (recorre el AST recursivamente). Maneja errores con fail. Guarda este prompt exacto en un archivo original_prompt.md dentro del proyecto.
