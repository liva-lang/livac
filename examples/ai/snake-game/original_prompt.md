Implementa el juego Snake en la terminal en Liva (.liva) usando rust { } interop con crossterm o similar para input no-bloqueante y control del cursor. La serpiente se mueve con WASD, come comida que aparece aleatoriamente (usa Math.random()), crece al comer, y el juego termina al chocar con los bordes o consigo misma. Muestra puntuación actual, velocidad progresiva, y permite reiniciar al terminar. Renderiza el tablero con caracteres ASCII. Guarda este prompt exacto en un archivo original_prompt.md dentro del proyecto.
