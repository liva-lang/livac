Crea un motor de búsqueda de texto en Liva (.liva) que indexe todos los archivos .txt de un directorio usando Dir.list() y File.read(). Debe construir un índice invertido (Map de palabra → lista de archivos), soportar búsquedas por palabra individual y operadores AND/OR. Muestra resultados ordenados por frecuencia de aparición. Usa Maps, arrays, split(), filter() y sort(). Guarda este prompt exacto en un archivo original_prompt.md dentro del proyecto.
