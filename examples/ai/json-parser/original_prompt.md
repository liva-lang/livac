Implementa un serializador/deserializador JSON desde cero en Liva (.liva). Debe soportar strings, números, booleanos, null, arrays y objetos anidados. Incluye pretty-printing con indentación configurable. Implementa un lexer que tokenice el input y un parser recursivo. Usa pattern matching (switch), enums con datos, y recursión. Guarda este prompt exacto en un archivo original_prompt.md dentro del proyecto.
