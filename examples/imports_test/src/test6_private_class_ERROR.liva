// ============================================================================
// Test 6: Private Class Import (SHOULD FAIL TO COMPILE)
// ============================================================================
// This test SHOULD produce a compilation error because _InternalCounter is private

import { Circle, _InternalCounter } from "./lib/shapes.liva"

main() {
    print("=== Test 6: Private Class Import ===")
    
    // This should NOT work - _InternalCounter is private!
    let counter = _InternalCounter()
    counter.increment()
    print($"Counter: {counter.get()}")
    
    print("❌ FAILED: Compiler should have rejected private class import!")
}
