// ============================================================================
// Test 5: Private Function Import (SHOULD FAIL TO COMPILE)
// ============================================================================
// This test SHOULD produce a compilation error because _power is private

import { add, _power } from "./lib/math.liva"

main() {
    print("=== Test 5: Private Function Import ===")
    
    // This should NOT work - _power is private!
    print($"_power(2, 8) = {_power(2, 8)}")
    
    print("❌ FAILED: Compiler should have rejected private import!")
}
