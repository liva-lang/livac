// ============================================================================
// Test 8: Public Functions That Use Private Helpers
// ============================================================================
// Public functions can internally use private helpers - this should work

import { square, cube, power } from "./lib/math.liva"
import { wrap, repeat } from "./lib/strings.liva"

main() {
    print("=== Test 8: Public Functions Using Private Helpers ===")
    print("")
    
    // square, cube, power are simple functions in our simplified test
    print("Math functions:")
    print($"  square(5) = {square(5)}")
    print($"  cube(3) = {cube(3)}")
    print($"  power(2, 10) = {power(2, 10)}")
    print("")
    
    // wrap, repeat are string functions
    print("String functions:")
    print($"  wrap('x', '[', ']') = '{wrap('x', '[', ']')}'")
    print($"  repeat('ab', 4) = '{repeat('ab', 4)}'")
    
    print("")
    print("✅ Test 8 PASSED: Public functions using private helpers work!")
}
