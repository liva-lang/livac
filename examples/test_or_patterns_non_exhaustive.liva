// Test: Non-exhaustive or-pattern (should fail with E0902)

main() {
    let num = 7;
    let category = switch num {
        1 | 2 | 3 => "small",
        4 | 5 | 6 => "medium"
        // Missing wildcard - should trigger E0902
    };
}
