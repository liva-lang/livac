// Stdlib Test: Crypto — sha256, base64Encode (self-hosted subset)
// NOTE: md5 excluded (wrong crate: md5 v0.7 vs md-5 v0.10)
// NOTE: base64Decode excluded (double-match tuple/Result conflict)
import { describe, test, expect } from "liva/test"

describe("Crypto SHA256", () => {
    test("sha256 empty string", () => {
        let hash = Crypto.sha256("")
        expect(hash).toBe("e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855")
    })

    test("sha256 hello", () => {
        let hash = Crypto.sha256("hello")
        expect(hash).toBe("2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824")
    })
})

describe("Crypto Base64", () => {
    test("base64 encode", () => {
        let encoded = Crypto.base64Encode("hello world")
        expect(encoded).toBe("aGVsbG8gd29ybGQ=")
    })

    test("base64 encode empty", () => {
        let encoded = Crypto.base64Encode("")
        expect(encoded).toBe("")
    })
})
