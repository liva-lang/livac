// E2E Test: Fibonacci
// Recursive fibonacci function

fib(n: number): number {
    if n <= 0 {
        return 0
    }
    if n == 1 {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}

main() {
    for i in 0..10 {
        print($"fib({i}) = {fib(i)}")
    }
}
