// String utility module — exported functions
greet(name: string): string => $"Hello, {name}!"

repeat(s: string, n: number): string {
    let result = ""
    let i = 0
    while i < n {
        result = result + s
        i = i + 1
    }
    return result
}

capitalize(s: string): string => s.toUpperCase()
