// Lowest-level helpers; no imports.

clamp(x: number, lo: number, hi: number): number {
    if x < lo { return lo }
    if x > hi { return hi }
    return x
}

abs(x: number): number {
    if x < 0 { return -x }
    return x
}
